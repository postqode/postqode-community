Create `requirements.md` for a web app called `wordfreq`.

<task>
Single page with a textarea for pasting text, a numeric input for N, and a submit button.
On submit, return the top N most frequent words (case-insensitive, punctuation stripped), sorted by count descending then alphabetically for ties.
</task>

<instruction>
Before writing `requirements.md`, ask me clarifying questions about anything underspecified: language/framework, input size limits, storage/persistence, visual style, testing approach, dependency management, and any other decisions needed for a public-facing deployment. Ask all questions in a single batch, in one message. Once I reply, treat my reply as complete and final, even if it doesn't address every question you asked. Do not ask follow-up questions. For anything still unaddressed after my reply, make a reasonable default choice yourself, state it explicitly in requirements.md, and proceed.
</instruction>

<constraints>
Do not write any code. Save output to `requirements.md`.
</constraints>
