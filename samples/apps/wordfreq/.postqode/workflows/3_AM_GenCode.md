<context>
Read `design.md`, specifically the Resolved Decisions section.
</context>

<task>
Implement the app exactly as specified: routes, function signatures, visual style tokens, input validation, and the character cap, using the language, framework, and server configuration fixed in Resolved Decisions. Return a clear error response, not a crash, for oversized or empty input.
</task>

<constraints>
Do not write any test files or test code, even minimal or sanity checks. Testing is handled entirely in a separate stage. Do not deviate from the design without flagging it first.
</constraints>