<context>
Read `requirements.md`.
</context>

<task>
Produce `design.md` covering: route(s) and request/response shape, the tokenizing/counting algorithm, tie-breaking logic, and input validation rules.
</task>

<instruction>
Include a "Resolved Decisions" section at the top of design.md that explicitly restates every constraint from requirements.md as fixed values: language/framework, character cap, storage rules, visual style tokens, port handling, testing library, and dependency management approach. This section is the authoritative source for later stages, and later stages must use these exact tools and commands, not alternatives.
</instruction>

<constraints>
Only function signatures, route definitions, and short descriptions per function. No implementation code. Save output to `design.md`.
</constraints>